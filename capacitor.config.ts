import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TermometroGaiborJuan3E2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
